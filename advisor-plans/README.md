# Implementation Plans

Generated by the `improve` skill (`branch`) across two sessions, planned at commit `b41f1e96` (both sessions).

**Session 1** (2026-07-11): rescoped to the uncommitted working-tree diff at the time (15 modified files + 1 untracked file). Produced plans 001-003.

**Session 2** (2026-07-11): rescoped to the 16 *committed-but-unpushed* commits ahead of the remote tracking branch (`git diff 906fb406..b41f1e96`, 53 files, +3378/-1035) — deliberately excluding both the uncommitted working-tree diff (already covered above) and the 300+ commit divergence from `development` (out of scope; not "what's about to be pushed"). Produced plans 004-007.

`plans/` already exists in this repo for unrelated freeform planning docs (and is gitignored), so these live in `advisor-plans/` instead per the skill's fallback rule.

Execute in order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Remove leftover debug logging from HIDEventManager's right-click path | P2 | S | — | IN PROGRESS (all grep verifications passed; pre-existing unrelated build failure in MenuBarSectionController.swift/MenuBarRecoveryDriver.swift blocks the build-succeeds check) |
| 002 | Remove stray `default.profraw` and gitignore the pattern | P3 | S | — | DONE |
| 003 | Stop mislabeling non-launch failures as `HookError.launchFailed` in HookRunner | P3 | S | — | IN PROGRESS (all grep verifications passed; same pre-existing unrelated build failure blocks the build-succeeds check) |
| 004 | Make `MenuBarItemImageCache.captureImages` actually respect its `freshBounds` parameter | P1 | S | — | IN PROGRESS (implementation and regression tests complete; full suite blocked by the pre-existing PlatformRuntimeKit binary/source symbol mismatch) |
| 005 | Arm self-change suppression windows before the mutation that can trigger them | P1 | S | — | IN PROGRESS (implementation and contract test complete; full suite blocked by the pre-existing PlatformRuntimeKit binary/source symbol mismatch) |
| 006 | Add test coverage for the recovery-driver healthy-guard wiring | P2 | M | — | BLOCKED (plan STOP condition: target handlers and driver are private, and the existing nil-AppState fixture provides no recovery seam without production visibility changes or new harness infrastructure) |
| 007 | Add test coverage for `ThawCapture`'s stream-rebind decision logic | P2 | M | — | DONE (`swift test --package-path ThawCapture` passes; full app suite remains independently blocked by the pre-existing PlatformRuntimeKit binary/source symbol mismatch) |

No dependencies between any of these seven — all independent, all safe to execute in any order or in parallel. (004-007 touch different files than 001-003, so the two batches don't conflict either.)
